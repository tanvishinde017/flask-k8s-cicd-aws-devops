apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: LoadBalancer   # 🔥 CHANGE THIS
  selector:
    app: flask
  ports:
    - port: 80         # external port
      targetPort: 5000 # container port